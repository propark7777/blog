create table plan (
 exercise_id bigint generated by default as identity,
 exercise_name varchar(30),
 exercise_set_number integer not null default 0,
 exercise_repetition integer not null default 0,
 primary key (exercise_id)
);